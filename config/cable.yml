# Action Cable uses Redis by default to administer connections, channels, and sending/receiving messages over the WebSocket.
production:
  adapter: redis
  url: redis://:chat_room@pub-redis-14572.us-east-1-2.1.ec2.garantiadata.com:14572

development:
  adapter: async

test:
  adapter: async
